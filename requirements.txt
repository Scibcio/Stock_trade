# Stock_trade dependencies (pinned). Rebuild the venv with:
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# NOTE: torch is a CUDA build and is NOT on PyPI - install it separately:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128

pandas==3.0.3
numpy==2.4.6
requests==2.34.2
yfinance==1.4.1
pytz==2026.2
lxml==6.1.1
scikit-learn==1.9.0
scipy==1.17.1
xgboost==3.2.0
streamlit==1.58.0
pytest==9.1.1
alpaca-py==0.43.5
python-dotenv==1.2.2
